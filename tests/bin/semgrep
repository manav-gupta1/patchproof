#!/usr/bin/env python3
import json
from pathlib import Path

src = Path("app/db.py").read_text()
vulnerable = ' + username + ' in src
result = {"results": []}
if vulnerable:
    result["results"].append({
        "check_id": "patchproof.python.sql-injection",
        "path": "app/db.py",
        "start": {"line": 4},
        "end": {"line": 4},
        "extra": {"severity": "ERROR", "message": "User-controlled SQL concatenation"}
    })
print(json.dumps(result))
