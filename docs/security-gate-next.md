# Security gate next step

Checked the repository and runtime for an existing Semgrep configuration and
runnable Semgrep module/executable.

The security gate remains fail-closed. Verification and durable-verification
tests were run independently so their status is not conflated with the
missing static-analysis dependency.
