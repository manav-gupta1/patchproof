# API symbol map

Mapped remaining failing imports to actual repository definitions before
changing package exports. Symbols that do not exist in production code are
left unresolved rather than replaced with placeholders.
