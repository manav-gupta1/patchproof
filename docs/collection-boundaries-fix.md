# Collection boundary fix

Implemented the concrete evidence bundle builder API, exported the existing
orchestration/GitHub/context types, and made context extraction reject paths
that escape the repository root.
