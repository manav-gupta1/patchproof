# API reconciliation

Located the actual production definitions for missing test imports before
changing package exports. Also repaired the malformed SQL-injection fixture
conftest only where its contents were literal escaped newlines.

No placeholder production classes were introduced.
